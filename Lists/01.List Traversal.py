#https://www.geeksforgeeks.org/problems/list-traversal/1&selectedLang=python3

"""
print(i, end=" "): prints each element followed by a space instead of a newline.

print() at the end (optional): adds a newline after all elements are printed, so the terminal prompt doesn’t stay on the same line.
"""

def listTraversal(arr):
    for i in arr:
        print(i,end=" ")
